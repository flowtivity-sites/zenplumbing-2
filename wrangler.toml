name = "zenplumbing-2"
compatibility_date = "2025-01-15"

# Build configuration for Astro
[build]
command = "npm run build"

# Pages deployment
[site]
bucket = "./dist"

# Custom domain (optional)
# routes = [
#   { pattern = "zenplumbing-2.sites.flowtivity.com.au", custom_domain = true }
# ]
